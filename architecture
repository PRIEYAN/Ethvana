Ticket
  
  -ticketId - string
  -event - map{
    -eventName - string
    -eventDate - string
    -otherDetails - string
  }
  -organiser - map {
    -organiserId - address ,
    -organiserName - string ,
    -previousEvents - array
  }
  -scheduleBooking - Boolean (default:false)
  -schedule - map {
    -allowed - uint256
    -shortListDate - string
    -allotedTo - map
  }
  -maxBooking - uint256
  -BookedBy - map {
    -userId - address : date - string
  }
  -payment - map {
    -amount - uint256,
    -paid - array { address,.. }
  }
  
Users - 
  -userId - address
  -ethvanaEvent - map {
    -attended - array {},
    -notAttended - array {},
  }


ticket booking platform

same as normal booking platform,, but a audience books a ticket that is been pposted by 
an event organiser,, everyon can book the ticket,, there is 30% chance of prioriy for 
audience that have more reputation , or more nfts related to this event or 
dao score,, other 70% would be first come first server basis.

1729